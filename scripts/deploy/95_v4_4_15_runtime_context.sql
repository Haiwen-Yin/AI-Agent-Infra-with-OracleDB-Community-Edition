-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_runtime_context.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_RUNTIME_CONTEXT_BINDINGS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_RUNTIME_CONTEXT_BINDINGS (EXECUTION_ID VARCHAR(128) PRIMARY KEY REFERENCES CX_RUNTIME_EXECUTIONS(EXECUTION_ID), ASSEMBLY_ID VARCHAR(128) NOT NULL UNIQUE REFERENCES CX_CONTEXT_ASSEMBLIES(ASSEMBLY_ID), REQUESTED_BY VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), AGENT_ID VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), SECURITY_DOMAIN_ID VARCHAR(128) NOT NULL REFERENCES CX_SECURITY_DOMAINS(SECURITY_DOMAIN_ID), INPUT_DIGEST VARCHAR(64) NOT NULL, REQUEST_DIGEST VARCHAR(64) NOT NULL, IDEMPOTENCY_KEY VARCHAR(128) NOT NULL, REASON VARCHAR(1000) NOT NULL, CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, UNIQUE(REQUESTED_BY,IDEMPOTENCY_KEY))'; END IF; END;
/

DECLARE n NUMBER; BEGIN SELECT COUNT(*) INTO n FROM USER_TABLES WHERE TABLE_NAME='CX_RUNTIME_CONTEXT_ATTEMPTS'; IF n=0 THEN EXECUTE IMMEDIATE 'CREATE TABLE CX_RUNTIME_CONTEXT_ATTEMPTS (EXECUTION_ID VARCHAR(128) PRIMARY KEY REFERENCES CX_RUNTIME_CONTEXT_BINDINGS(EXECUTION_ID), USE_ID VARCHAR(128) NOT NULL UNIQUE REFERENCES CX_CONTEXT_INPUT_USES(USE_ID), WORKER_ID VARCHAR(128) NOT NULL, NODE_ID VARCHAR(128) NOT NULL, FENCING_TOKEN INTEGER NOT NULL CHECK(FENCING_TOKEN>0), CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL)'; END IF; END;
/

CREATE OR REPLACE TRIGGER CX86_DD43255546748EED BEFORE DELETE OR UPDATE ON CX_RUNTIME_CONTEXT_BINDINGS BEGIN RAISE_APPLICATION_ERROR(-20886,'Continuity identity and history are immutable'); END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM USER_TAB_PRIVS_MADE WHERE TABLE_NAME='CX_RUNTIME_CONTEXT_BINDINGS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_RUNTIME_CONTEXT_BINDINGS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/

CREATE OR REPLACE TRIGGER CX86_2E127CB6B635A6AA BEFORE DELETE OR UPDATE ON CX_RUNTIME_CONTEXT_ATTEMPTS BEGIN RAISE_APPLICATION_ERROR(-20886,'Continuity identity and history are immutable'); END;
/

BEGIN FOR g IN (SELECT GRANTEE,PRIVILEGE FROM USER_TAB_PRIVS_MADE WHERE TABLE_NAME='CX_RUNTIME_CONTEXT_ATTEMPTS') LOOP EXECUTE IMMEDIATE 'REVOKE '||g.PRIVILEGE||' ON CX_RUNTIME_CONTEXT_ATTEMPTS FROM "'||REPLACE(g.GRANTEE,'"','""')||'"'; END LOOP; END;
/
